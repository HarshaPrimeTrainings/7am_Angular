import { EvenoddpipePipe } from './evenoddpipe.pipe';

describe('EvenoddpipePipe', () => {
  it('create an instance', () => {
    const pipe = new EvenoddpipePipe();
    expect(pipe).toBeTruthy();
  });
});
